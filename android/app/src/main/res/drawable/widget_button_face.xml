<?xml version="1.0" encoding="utf-8"?>
<!-- A resting control: the raised panel tone, held by a hairline. The filled variants
     (widget_button_energy / _hero / _recovery) carry a PULSE gradient instead. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/widget_button_face" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="@color/widget_hairline" />
</shape>
